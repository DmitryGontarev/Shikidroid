package com.shikidroid.uikit

import androidx.compose.ui.graphics.Color
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp


/**
 * Стандартный стиль текста для приложения
 */
val StandardTextStyle13SP = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.Normal,
    fontSize = 13.sp
)

/**
 * Полужирный стиль текста для приложения
 */
val SemiBoldTextStyle13SP = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.SemiBold,
    fontSize = 13.sp
)

/**
 * Стандартный стиль текста для приложения
 */
val StandardTextStyle12SP = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.Normal,
    fontSize = twelveSP
)

/**
 * Стандартный стиль текста чёрного цвета для приложения
 */
val StandardBlackTextStyle = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.Normal,
    fontSize = twelveSP,
    color = Color.Black
)

/**
 * Стандартный стиль текста чёрного цвета для приложения
 */
val StandardGrayTextStyle = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.Normal,
    fontSize = twelveSP,
    color = Color.Gray
)

/**
 * Полужирный стиль текста для приложения
 */
val SemiBoldTextStyle12SP = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.SemiBold,
    fontSize = twelveSP
)

/**
 * Полужирный стиль текста большого размера для приложения
 */
val StandardBigTextStyle = TextStyle(
    fontFamily = FontFamily.Default,
    fontWeight = FontWeight.Normal,
    fontSize = sixteenSP,
    color = Color.Black
)

/**
 * Полужирный стиль текста для экрана заставки с названием приложения
 */
val SplashScreenTitle = TextStyle(
    fontFamily = FontFamily.SansSerif,
    fontWeight = FontWeight.SemiBold,
    fontSize = thirtySP,
    color = Color.Black
)